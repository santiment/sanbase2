#!/bin/sh

export RELEASE_COOKIE=$NODE_COOKIE
export RELEASE_DISTRIBUTION=name
export POD_A_RECORD=$(echo $POD_IP | sed 's/\./-/g')
export RELEASE_NODE="sanbase@${POD_A_RECORD}.${POD_NAMESPACE}.pod.cluster.local"